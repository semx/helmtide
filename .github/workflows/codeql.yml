name: CodeQL

# Code scanning is free on public repositories. On a private one it needs GitHub
# Advanced Security, and both this and the SARIF uploads in hadolint.yml and
# container-check.yml will fail without it.
on:
  pull_request:
    branches: [ main ]
    paths:
      - '**.go'
  schedule:
    - cron: '31 10 * * 5'

jobs:
  codeql:
    permissions:
      security-events: write
      contents: read
    name: Analyze
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        language: [ 'go' ]

    steps:
    - name: Checkout repository
      uses: actions/checkout@v6

    # Initializes the CodeQL tools for scanning.
    - name: Initialize CodeQL
      uses: github/codeql-action/init@v4
      with:
        languages: ${{ matrix.language }}

    - name: Autobuild
      uses: github/codeql-action/autobuild@v4


    - name: Perform CodeQL Analysis
      uses: github/codeql-action/analyze@v4
